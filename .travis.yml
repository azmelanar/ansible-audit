---
#
# This build script is provided by: https://github.com/azmelanar/ansible-audit
# Author: Dmitriy Slupytskyi
#
# For any issues of feedback report it at Github
#

language: python
python:
  - "2.7"

env:
  - ANSIBLE_VERSION=1.8

before_install:
  - sudo apt-get update -qq

install:
  # Install Ansible.
  - pip install ansible

  # Add ansible.cfg to pick up roles path.
  - printf "[defaults]\nansible_managed = This file is generated by Ansible. Changes will be overwritten.\nroles_path = ../\n" > ansible.cfg

script:
  # Check the role/playbook's syntax.
  - ansible-playbook -i tests/inventory tests/test.yml --syntax-check

  # Run the role/playbook with ansible-playbook.
  - ansible-playbook -i tests/inventory tests/test.yml --connection=local --sudo -vvvv
